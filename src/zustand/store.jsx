import { produce } from "immer";
import { create } from "zustand";


export const useStore = create((set) => ({
    //list : []
    //function : (list) => 
        /* set(produce((state)=> {
                state.list.push()
            }))
        
            */
}))



// to use state 
{/*
   const list = useStore((state)=> (state.list)) // list usage
   const function_name = useStore((state)=> (state.function_name)) // function usage
*/}